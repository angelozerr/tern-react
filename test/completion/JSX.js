var util = require("./../util");

exports['test JSX (TODO) completion'] = function() {
  
  // Uncomment when tern 1.6 will be released.
  /*util.assertCompletion("var content = <Container>{window.isLoggedIn ? <Nav /> : <Login />}</Container>;", {
    "name":"chain",
    "type":"fn(tweens: Phaser.Tween) -> Phaser.Tween",
    "origin":"phaser"
  });*/

  
}
